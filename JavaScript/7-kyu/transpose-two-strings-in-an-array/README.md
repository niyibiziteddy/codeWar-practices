<h2><a href=https://www.codewars.com/kata/581f4ac139dc423f04000b99/train/javascript target="_blank">Transpose two strings in an array</a></h2><h3>7 kyu</h3><p>You will be given an array that contains two strings. Your job is to create a function that will take those two strings and transpose them, so that the strings go from top to bottom instead of left to right.</p><p>Formatting:</p><ul><li>There should be one space in between the two characters</li><li>If one string is longer than the other, there should be a space where the character would be</li><li>Lines should be separated by a newline character. The last line should not contain a trailing newline.</li></ul><h3 id="examples">Examples</h3><pre><code class="language-javascript">[<span class="cm-string">'Hello'</span>,<span class="cm-string">'World'</span>]<span class="cm-comment">--&gt;</span><span class="cm-string-2">`</span><span class="cm-string-2">H W</span><span class="cm-string-2">e o</span><span class="cm-string-2">l r</span><span class="cm-string-2">l l</span><span class="cm-string-2">o d`</span><span class="cm-comment">// or, with escape sequences:</span><span class="cm-string">"H W\ne o\nl r\nl l\no d"</span></code></pre><p>Mismatched lengths:</p><pre><code class="language-javascript">[<span class="cm-string">'ab'</span>, <span class="cm-string">'123'</span>] <span class="cm-operator">--&gt;</span><span class="cm-string-2">`</span><span class="cm-string-2">a 1</span><span class="cm-string-2">b 2</span>  <span class="cm-string-2">3`</span>  <span class="cm-comment">// or, with escape sequences:</span><span class="cm-string">"a 1\nb 2\n  3"</span></code></pre>